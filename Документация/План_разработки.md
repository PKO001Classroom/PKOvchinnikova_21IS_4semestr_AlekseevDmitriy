## 📋 План разработки 

### 🎯 **Цель плана**
Разработать и развернуть веб-приложение для управления учебным процессом в соответствии с ФГОС СПО, используя Docker для контейнеризации, обеспечения воспроизводимости и упрощения деплоя.

---

### ✅ **Этап 0: Подготовка (Неделя 1)**

**Задачи:**
1. **Настройка репозитория Git**
   - Создать репозиторий с README, .gitignore (Python, Django, Docker)
   - Структура папок согласно `edu_system/` из архитектурного решения
2. **Написание Dockerfile и docker-compose.yml**
   - Базовый образ Python 3.11
   - Установка зависимостей из `requirements.txt`
   - Настройка PostgreSQL и Nginx в compose
3. **Настройка CI/CD (опционально)**
   - GitHub Actions для автоматической сборки и тестирования

---

### ✅ **Этап 1: Базовая настройка Django + модели БД (Неделя 2)**

**Задачи:**
1. **Инициализация Django-проекта**
   - `django-admin startproject edu_system`
   - Настройка `settings.py` для работы с PostgreSQL
2. **Создание приложений Django**
   - `users` (пользователи, роли, группы)
   - `grades` (оценки, комментарии)
   - `subjects` (предметы, компетенции ФГОС)
3. **Миграции моделей**
   - Реализация таблиц из архитектурного решения
   - Создание фикстур для тестовых данных (компетенции ФГОС, предметы)
4. **Docker-сборка и запуск**
   - Проверка работы контейнеров
   - Настройка переменных окружения через `.env`

---

### ✅ **Этап 2: Система авторизации и роли (Неделя 3)**

**Задачи:**
1. **Реализация авторизации (UC-1)**
   - Кастомная модель User с полями: роль, специальность, группа
   - Форма входа с валидацией
2. **Ролевой доступ (RBAC)**
   - Декораторы/миксины для проверки ролей
   - Ограничение доступа студентов к выставлению оценок
3. **Тестирование в Docker**
   - Запуск тестов в контейнере (`docker-compose run web pytest`)

---

### ✅ **Этап 3: Личный кабинет студента (Неделя 4)**

**Задачи:**
1. **Шаблоны и маршруты**
   - `/student/dashboard` — дашборд с предметами и средними баллами
   - `/student/grades` — таблица оценок с фильтрами
   - `/student/report` — отчёт в PDF (с использованием `reportlab` или `weasyprint`)
2. **Бизнес-логика**
   - Расчёт среднего балла (функция `calculate_average`)
   - Валидация комментариев (минимум 100 символов)
3. **Docker-тестирование интерфейса**
   - Запуск Selenium-тестов в контейнере

---

### ✅ **Этап 4: Панель преподавателя (Неделя 5)**

**Задачи:**
1. **Шаблоны и маршруты**
   - `/teacher/dashboard` — список групп и предметов
   - `/teacher/grade/add` — форма выставления оценки по ФГОС
   - `/teacher/reports` — аналитика и экспорт
2. **Логика выставления оценок**
   - Автоматический расчёт оценки по проценту освоения
   - Ограничение: 1 оценка в неделю (проверка через `Constraint`)
3. **Docker-команды для администрирования**
   - `docker-compose run web python manage.py create_teacher`
   - `docker-compose run web python manage.py import_fgos`

---

### ✅ **Этап 5: Валидации по ФГОС + PDF-отчёты (Неделя 6)**

**Задачи:**
1. **Реализация ФГОС-логики**
   - Таблица `fgos_competencies`
   - Привязка оценок к кодам ПК/ОПК
2. **Генерация PDF**
   - Отчёт по успеваемости студента
   - Экспорт для аттестационной комиссии
3. **Docker-образ для генерации PDF**
   - Добавление системных зависимостей (fonts, libs)

---

### ✅ **Этап 6: Интеграция Nginx + статика (Неделя 7)**

**Задачи:**
1. **Настройка Nginx**
   - Проксирование запросов к Django
   - Обслуживание статических и медиафайлов
2. **Сборка статики в Docker**
   - `docker-compose run web python manage.py collectstatic`
   - Volume для статики
3. **Тестирование производительности**
   - Нагрузочное тестирование с использованием `locust` в контейнере

---

### ✅ **Этап 7: Тестирование, фиксы, документация (Неделя 8)**

**Задачи:**
1. **Полное тестирование**
   - Unit-тесты (pytest)
   - Интеграционные тесты (Selenium в Docker)
   - Нагрузочные тесты (Locust)
2. **Документация**
   - README с инструкцией по запуску через Docker
   - API-документация (если нужно)
   - Деплой-инструкции (Docker + сервер)
3. **Подготовка к деплою**
   - Оптимизация Docker-образов (multi-stage build)
   - Настройка `docker-compose.prod.yml`

---

### ✅ **Этап 8: Деплой (Неделя 9)**

**Задачи:**
1. **Выбор хостинга**
   - VPS (DigitalOcean, Hetzner) или облако (Yandex Cloud)
2. **Настройка сервера**
   - Установка Docker и Docker Compose
   - Копирование `docker-compose.prod.yml` и `.env`
3. **Запуск в production**
   - `docker-compose -f docker-compose.prod.yml up -d`
   - Настройка домена и SSL (Certbot в Nginx)

---

## 📦 **Примерный docker-compose.yml (разработка)**

```yaml
version: '3.8'

services:
  db:
    image: postgres:13
    environment:
      POSTGRES_DB: edu_system
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
    volumes:
      - postgres_data:/var/lib/postgresql/data

  web:
    build: .
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    depends_on:
      - db
    environment:
      DATABASE_URL: postgres://user:password@db:5432/edu_system

  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf
      - static_volume:/app/static
    depends_on:
      - web

volumes:
  postgres_data:
  static_volume:
```

---

## ✅ **Критерии успешного завершения плана**

1. ✅ Все этапы реализованы в соответствии с бизнес-требованиями (ФГОС, роли, валидации)
2. ✅ Приложение полностью работает в Docker (локально и на сервере)
3. ✅ Реализованы все ключевые страницы и функции из ТЗ и FRD
4. ✅ Проведено тестирование (unit, интеграционное, нагрузочное)
5. ✅ Документация и инструкции по запуску готовы

